'use strict';

// FUNCTIONS //


// CDF //

/**
* FUNCTION: cdf( x, a, b )
*	Evaluates the cumulative distribution function (CDF) for a Uniform distribution with minimum value `a` and maximum value `b` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} a - minimum value
* @param {Number} b - maximum value
* @returns {Number} evaluated CDF
*/
function cdf( x, a, b ) {

} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;
